//this is a list of objects that always activate instances in a square region around them
//useful to prevent bugs where objects get stuck in blocks and such

instance_activate_around(Player,64)
instance_activate_around(Platform,64)
instance_activate_around(CherryBounce,64)
instance_activate_around(Bullet,64)
